import { DatesCustomPipe } from './dates-custom.pipe';

describe('DatesCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new DatesCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
